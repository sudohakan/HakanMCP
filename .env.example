# =============================================================================
# HakanMCP — Secrets (config.yaml = settings, .env = keys/tokens)
# =============================================================================

# GitHub (required when config.github.enabled: true)
GITHUB_TOKEN=

# AI API Keys (optional)
GEMINI_API_KEY=

# GitBook (optional)
GITBOOK_URL=
GITBOOK_TOKEN=
