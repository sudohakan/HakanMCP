{
  "version": "1.0",
  "description": "On-demand MCP server catalog. Servers with envKeys load credentials from ~/.credentials.env automatically.",
  "servers": {
    "fetch": {
      "name": "Fetch",
      "description": "Enhanced web content fetching and markdown conversion",
      "command": "uvx",
      "args": ["mcp-server-fetch"],
      "conditions": [
        "WebFetch returns an error or truncates content",
        "Page is too large and needs chunked retrieval",
        "Raw HTML content needed without markdown conversion"
      ]
    },
    "filesystem": {
      "name": "Filesystem",
      "description": "Sandboxed file operations with directory tree, batch read, and search",
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-filesystem"],
      "dynamicArgs": ["allowedDirectories"],
      "conditions": [
        "Recursive directory tree visualization needed",
        "Batch reading multiple files at once",
        "Sandboxed file access to a specific directory required"
      ]
    },
    "git": {
      "name": "Git",
      "description": "Deep git repository analysis - diff, blame, log, branch comparison",
      "command": "uvx",
      "args": ["mcp-server-git"],
      "conditions": [
        "Cross-branch file comparison or multi-commit diff analysis",
        "Blame, log search, or commit history pattern analysis",
        "Bash git commands insufficient for complex operations"
      ]
    },
    "graphify": {
      "name": "Graphify",
      "description": "Codebase knowledge graph - query structure, find god nodes, trace paths between concepts",
      "command": "/home/hakan/.graphify-venv/bin/python",
      "args": ["-m", "graphify.serve", "/mnt/c/dev/graphify/graphify-out/graph.json"],
      "conditions": [
        "Querying codebase structure, architecture, or relationships",
        "Finding connections between code concepts",
        "Understanding god nodes and community structure"
      ]
    },
    "sequential-thinking": {
      "name": "Sequential Thinking",
      "description": "Structured reasoning with hypothesis revision and trade-off analysis",
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-sequential-thinking"],
      "conditions": [
        "Architectural decision with 3+ alternatives requiring trade-off analysis",
        "Debugging requires systematic hypothesis testing with revision",
        "Deep analysis, comparison, or multi-step reasoning needed"
      ]
    },
    "sqlite": {
      "name": "SQLite",
      "description": "Local SQLite database querying and analysis",
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-sqlite"],
      "dynamicArgs": ["dbPath"],
      "conditions": [
        "Project contains .sqlite or .db files to query",
        "Local database analysis or data extraction needed",
        "Dedicated SQL tools needed for SQLite"
      ]
    },
    "time": {
      "name": "Time",
      "description": "Timezone conversion and current time queries",
      "command": "uvx",
      "args": ["mcp-server-time"],
      "conditions": [
        "User asks to convert time between timezones",
        "Cross-timezone scheduling calculation needed",
        "Current time in a specific timezone needed"
      ]
    },
    "mermaid": {
      "name": "Mermaid",
      "description": "Diagram generation - flowcharts, sequence, class, ER, state, Gantt",
      "command": "npx",
      "args": ["-y", "mcp-mermaid"],
      "conditions": [
        "User asks for a visual diagram",
        "Architecture or data flow visualization needed",
        "Documentation requires embedded diagrams"
      ]
    },
    "duckdb": {
      "name": "DuckDB",
      "description": "Analytical SQL on local CSV, Parquet, and JSON files",
      "command": "uvx",
      "args": ["mcp-server-duckdb"],
      "dynamicArgs": ["dbPath"],
      "conditions": [
        "SQL queries on local CSV, Parquet, or JSON files needed",
        "Analytical queries (aggregation, joins, window functions) on file-based data",
        "In-memory analytical database for data exploration"
      ]
    },
    "playwright": {
      "name": "Playwright",
      "description": "Browser automation and lightweight browser evidence capture",
      "command": "npx",
      "args": ["-y", "@playwright/mcp@latest"],
      "conditions": [
        "A browser-only flow must be automated through MCP",
        "JavaScript-heavy pages require focused browser evidence",
        "You want low-token browser wrappers on top of a Playwright MCP server"
      ]
    },
    "infoset": {
      "name": "Infoset",
      "description": "Customer support ticket, contact, and company management",
      "command": "node",
      "args": ["/mnt/c/dev/infoset-mcp/src/mcp-server.mjs"],
      "envKeys": ["INFOSET_API_KEY", "INFOSET_BASE_URL"],
      "conditions": [
        "Infoset ticket, contact, or company lookup",
        "Customer support operations",
        "SLA breach monitoring"
      ]
    },
    "kali": {
      "name": "Kali MCP",
      "description": "Offensive security tools — recon, scanning, exploitation",
      "command": "python3",
      "args": ["/mnt/c/dev/kali-mcp/main.py", "--transport", "stdio"],
      "conditions": [
        "Pentest, recon, or vulnerability scanning requested",
        "Network discovery or port scanning needed",
        "Security assessment or exploitation"
      ]
    },
    "chroma": {
      "name": "Chroma",
      "description": "Vector database for document embedding and similarity search",
      "command": "chroma-mcp",
      "args": ["--client-type", "persistent", "--data-dir", "/home/hakan/.chroma-data"],
      "conditions": [
        "Vector database or embedding operations",
        "Document similarity search",
        "RAG pipeline or knowledge base queries"
      ]
    },
    "markitdown": {
      "name": "MarkItDown",
      "description": "Convert any file to Markdown (PDF, DOCX, PPTX, XLSX, HTML, images, audio, ZIP, YouTube)",
      "command": "uvx",
      "args": ["markitdown-mcp"],
      "conditions": [
        "Read tool cannot handle binary files (PDF, DOCX, PPTX, XLSX)",
        "File content needs to be converted to Markdown for analysis",
        "Image OCR or audio transcription to text needed",
        "YouTube video content extraction needed"
      ]
    },
    "docker": {
      "name": "Docker MCP Gateway",
      "description": "Docker MCP gateway - container and image operations via docker mcp toolset",
      "command": "/mnt/c/Program Files/Docker/Docker/resources/bin/docker.exe",
      "args": ["mcp", "gateway", "run"],
      "conditions": [
        "Docker container or image management through MCP needed",
        "Docker MCP toolset operations required",
        "Bash docker commands insufficient for the task"
      ]
    },
    "gtasks": {
      "name": "Google Tasks",
      "description": "Google Tasks list/create/update (OAuth — requires prior auth in config dir)",
      "command": "/home/hakan/.bun/bin/bun",
      "args": ["/mnt/c/dev/gtasks-mcp/dist/index.js"],
      "env": { "GTASKS_MCP_CONFIG_DIR": "/home/hakan/.config/gtasks-mcp" },
      "conditions": [
        "Google Tasks list, create, or update operations",
        "Task tracking via Google Tasks needed",
        "NOTE: OAuth — only works if config dir already authenticated"
      ]
    }
  }
}
