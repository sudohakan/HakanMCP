# HakanMCP Deep Regression Test Suite
# Docker Compose orchestration for comprehensive testing

version: '3.8'

services:
  # Main test runner
  test-runner:
    build:
      context: ../..
      dockerfile: tests/docker-regression/Dockerfile
    container_name: hakanmcp-test-runner
    volumes:
      - test-data:/app/data
      - test-reports:/app/reports
      - test-logs:/app/logs
    environment:
      - NODE_ENV=test
      - HAKANMCP_MODE=test
      - HAKANMCP_MEMORY_PATH=/app/data
      - HAKANMCP_LOG_LEVEL=debug
      - HAKANMCP_MAX_AGENTS=15
      - HAKANMCP_SECURITY_MODE=strict
      - HAKANMCP_HNSW_M=16
      - HAKANMCP_HNSW_EF=200
      - HAKANMCP_EMBEDDING_DIM=384
      - CI=true
      - TEST_REPORT_PATH=/app/reports
      - MCP_SERVER_HOST=localhost
      - MCP_SERVER_PORT=3000
    networks:
      - hakanmcp-test
    command: ["bash", "/app/tests/docker-regression/scripts/run-all-tests.sh"]

  # MCP Server for integration tests
  mcp-server:
    build:
      context: ../..
      dockerfile: tests/docker-regression/Dockerfile
    container_name: hakanmcp-mcp-server
    volumes:
      - test-data:/app/data
      - test-logs:/app/logs
    environment:
      - NODE_ENV=test
      - HAKANMCP_MODE=test
      - HAKANMCP_MEMORY_PATH=/app/data
      - HAKANMCP_LOG_LEVEL=info
      - MCP_PORT=3000
    ports:
      - "3000:3000"
    networks:
      - hakanmcp-test
    healthcheck:
      test: ["CMD", "nc", "-z", "localhost", "3000"]
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 30s
    command: ["npx", "hakanmcp", "mcp", "start", "--port", "3000"]

  # Unit test runner
  unit-tests:
    build:
      context: ../..
      dockerfile: tests/docker-regression/Dockerfile
    container_name: hakanmcp-unit-tests
    volumes:
      - test-reports:/app/reports
    environment:
      - NODE_ENV=test
      - CI=true
      - TEST_REPORT_PATH=/app/reports
    networks:
      - hakanmcp-test
    command: ["bash", "/app/tests/docker-regression/scripts/run-unit-tests.sh"]

  # Integration test runner
  integration-tests:
    build:
      context: ../..
      dockerfile: tests/docker-regression/Dockerfile
    container_name: hakanmcp-integration-tests
    volumes:
      - test-data:/app/data
      - test-reports:/app/reports
    environment:
      - NODE_ENV=test
      - HAKANMCP_MODE=test
      - HAKANMCP_MEMORY_PATH=/app/data
      - CI=true
      - TEST_REPORT_PATH=/app/reports
      - MCP_SERVER_HOST=mcp-server
      - MCP_SERVER_PORT=3000
    depends_on:
      mcp-server:
        condition: service_healthy
    networks:
      - hakanmcp-test
    command: ["bash", "/app/tests/docker-regression/scripts/run-integration-tests.sh"]

  # Performance benchmark runner
  benchmark-tests:
    build:
      context: ../..
      dockerfile: tests/docker-regression/Dockerfile
    container_name: hakanmcp-benchmark-tests
    volumes:
      - test-data:/app/data
      - test-reports:/app/reports
    environment:
      - NODE_ENV=test
      - HAKANMCP_MODE=test
      - HAKANMCP_MEMORY_PATH=/app/data
      - CI=true
      - TEST_REPORT_PATH=/app/reports
    networks:
      - hakanmcp-test
    command: ["bash", "/app/tests/docker-regression/scripts/run-benchmark-tests.sh"]

  # Security test runner
  security-tests:
    build:
      context: ../..
      dockerfile: tests/docker-regression/Dockerfile
    container_name: hakanmcp-security-tests
    volumes:
      - test-reports:/app/reports
    environment:
      - NODE_ENV=test
      - HAKANMCP_SECURITY_MODE=strict
      - CI=true
      - TEST_REPORT_PATH=/app/reports
    networks:
      - hakanmcp-test
    command: ["bash", "/app/tests/docker-regression/scripts/run-security-tests.sh"]

networks:
  hakanmcp-test:
    driver: bridge

volumes:
  test-data:
    driver: local
  test-reports:
    driver: local
  test-logs:
    driver: local
